import React from 'react';

import ScreenPlaceholder from '../../components/ScreenPlaceholder';

function FilterGameScreen() {
  return (
    <ScreenPlaceholder
      title="Filter Game"
      owner="OWNER B — HOME / GAMES"
      description="Filter games by genre, platform, price or community score."
    />
  );
}

export default FilterGameScreen;
