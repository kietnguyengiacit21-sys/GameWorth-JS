import React from 'react';

import ScreenPlaceholder from '../../components/ScreenPlaceholder';

function GameMediaScreen() {
  return (
    <ScreenPlaceholder
      title="Game Media"
      owner="OWNER B — HOME / GAMES"
      description="Display cover artwork, screenshots and trailer links."
    />
  );
}

export default GameMediaScreen;
