import React from 'react';

import ScreenPlaceholder from '../../components/ScreenPlaceholder';

function SearchGameScreen() {
  return (
    <ScreenPlaceholder
      title="Search Game"
      owner="OWNER B — HOME / GAMES"
      description="Search the game list from Redux or a backend query endpoint."
    />
  );
}

export default SearchGameScreen;
